import SwiftUI

extension Color {
    static let metronomePrimary = Color("MetronomePrimary")
    static let dronePrimary     = Color("DronePrimary")
    static let beatActive       = Color("BeatActive")
    static let beatInactive     = Color("BeatInactive")
    static let accentStrong     = Color("AccentStrong")
    static let accentMedium     = Color("AccentMedium")
    static let accentWeak       = Color("AccentWeak")

    static let cardBackground   = Color(.secondarySystemBackground)
    static let primaryText      = Color(.label)
    static let secondaryText    = Color(.secondaryLabel)
}
